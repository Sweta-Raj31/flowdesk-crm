import React,{useEffect,useState}from'react';import api from'../api';
export default function Leads(){const[d,setD]=useState({items:[],total:0}),[q,setQ]=useState('');const load=()=>api.get('/leads',{params:{q,limit:20}}).then(r=>setD(r.data));useEffect(load,[]);return <><div className="title"><h1>Leads</h1><input placeholder="Search name, email, company" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&load()}/></div><section>{d.items.map(x=><div className="row" key={x._id}><strong>{x.name}</strong><span>{x.company}</span><span>{x.email}</span><span className="badge">{x.status}</span></div>)}</section></>}
